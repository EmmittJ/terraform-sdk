resource "aws_security_group" "web" {
  dynamic "ingress" {
    for_each = var.ingress_ports
    
    content {
      cidr_blocks = ["0.0.0.0/0"]
      from_port   = port.value
      protocol    = "tcp"
      to_port     = port.value
    }
  }
}